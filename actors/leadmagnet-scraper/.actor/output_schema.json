{
  "actorOutputSchemaVersion": 1,
  "title": "LeadMagnet Output",
  "description": "Business leads extracted from Google Maps are stored in the default dataset.",
  "properties": {
    "results": {
      "type": "string",
      "title": "Results (JSON/CSV)",
      "description": "Structured business leads data available in the default dataset. Export as JSON, CSV, Excel, XML, or HTML.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "mapView": {
      "type": "string",
      "title": "Map View",
      "description": "Interactive map showing scraped business locations from the key-value store.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/results-map.html"
    }
  }
}
